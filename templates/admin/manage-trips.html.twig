{% extends 'admin.html.twig' %}

{% block title %}Must Do Diving - {{ controller_name }} {% endblock %}

{% block body %}
 
<div class="container">
    <div class="col-md-8">
    {{ form_start(frmtrips) }}     
    <div class="row pt-5">
            <div class="col-md-5">{{ form_row(frmtrips.trip) }}</div>
            <div class="col-md-3">  {{ form_row(frmtrips.trip_month) }}</div>
            <div class="col-md-2"> {{ form_row(frmtrips.trip_year) }} </div>
            <div class="col-md-2">  {{ form_row(frmtrips.button) }} </div>                        
    </div>
    {{ form_end(frmtrips) }}
    <hr>

    
    
    
    
       
    

    <table class="table table-striped table-bordered" id="trips" width="100%">
                <thead>
                        <tr>
                                <th>Trips</th>
                                <th>Month</th>
                                <th>Year</th>
                                <th>Option</th>
                        </tr>
                </thead>
    
    </table>    


   </div>

    
</div>
{% endblock %}
