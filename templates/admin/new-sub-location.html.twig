<style type="text/css">
	.modal{
		width: 350px;
        height: 200px;
	}
    .modal a.close-modal{
        top: 12.5px;
        right: 12.5px;  
    }
</style>

        {% for message in app.flashes('info') %}
                <div class="alert alert-success mt-30">{{ message }}</div>
        {% endfor %}

<div class="container">

<h5>{{ controller_name }}</h5>
<hr>
{{ form_start(frmsublocation) }}

    {{form_row(frmsublocation.sublocation) }}

<hr>

    {{ form_row(frmsublocation.button) }}

{{ form_end(frmsublocation) }}


</div>